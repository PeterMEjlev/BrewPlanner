node_modules/
dist/
*.log

# SQLite database (per-device; contains live data + the admin password hash)
*.sqlite
# SQLite WAL/lock files (transient, can be inconsistent)
*.sqlite-shm
*.sqlite-wal

# Generated session-signing secret (per-device, never commit)
session-secret

# Editor / OS
.DS_Store
.vscode/
.idea/
